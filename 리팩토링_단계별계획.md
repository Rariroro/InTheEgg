# 펫 시스템 리팩토링 단계별 계획

## 목표
Action과 Controller를 통합하여 더 간단한 구조 만들기

## 1단계: WanderAction 리팩토링 (가장 단순)

### 1-1. Git 커밋
- [ ] 현재 상태를 Git에 커밋 (`git commit -m "리팩토링 전 상태"`)

### 1-2. Static Controller 생성
```csharp
// 새 파일: PetMovementController.cs (static으로 변경)
public static class PetMovementController
{
    // 공통 이동 관련 메서드들
    public static void SetRandomDestination(PetController pet, float radius = 30f) { }
    public static bool IsAtDestination(PetController pet) { }
    public static void StopMovement(PetController pet) { }
}
```

### 1-3. WanderAction 통합
- [ ] PetMovementController의 인스턴스 메서드들을 WanderAction으로 이동
- [ ] 코루틴은 `_pet.StartCoroutine()` 사용
- [ ] 공통 기능은 static PetMovementController 호출

### 1-4. 테스트
- [ ] 펫이 정상적으로 배회하는지 확인
- [ ] 다른 Action으로 전환이 잘 되는지 확인
- [ ] 메모리 누수 없는지 확인

### 1-5. 정리
- [ ] PetController.Awake()에서 PetMovementController 컴포넌트 제거
- [ ] 기존 PetMovementController 인스턴스 클래스 삭제

## 2단계: EatAction 리팩토링 (중간 복잡도)

### 2-1. Git 커밋
- [ ] 1단계 완료 후 커밋 (`git commit -m "WanderAction 리팩토링 완료"`)

### 2-2. Static Controller 생성
```csharp
// 새 파일: PetFeedingController.cs (static으로 변경)
public static class PetFeedingController
{
    // 공통 먹이 탐색 메서드들
    public static GameObject FindNearbyFood(PetController pet, float radius) { }
    public static bool CanEatFood(PetController pet, FoodType foodType) { }
}
```

### 2-3. EatAction 통합
- [ ] 먹이 찾기 로직 통합
- [ ] 먹기 코루틴 통합
- [ ] 움직이는 음식 추적 로직 통합

### 2-4. 테스트
- [ ] 음식 감지가 정상 작동하는지
- [ ] 움직이는 음식을 잘 쫓아가는지
- [ ] 먹기 애니메이션이 정상인지
- [ ] 배고픔 수치가 정상적으로 감소하는지

## 3단계: SleepAction 리팩토링

### 3-1. Git 커밋
- [ ] 2단계 완료 후 커밋 (`git commit -m "EatAction 리팩토링 완료"`)

### 3-2. Static Controller 생성
```csharp
// 새 파일: PetSleepingController.cs (static으로 변경)
public static class PetSleepingController
{
    public static GameObject FindNearbySleepArea(PetController pet, float radius) { }
}
```

### 3-3. SleepAction 통합
- [ ] 수면 장소 찾기 로직 통합
- [ ] 수면 코루틴 통합

### 3-4. 테스트
- [ ] 수면 장소를 잘 찾는지
- [ ] 수면 애니메이션이 정상인지
- [ ] 졸림 수치가 정상적으로 감소하는지

## 4단계: ClimbTreeAction 리팩토링 (가장 복잡)

### 4-1. Git 커밋
- [ ] 3단계 완료 후 커밋 (`git commit -m "SleepAction 리팩토링 완료"`)

### 4-2. Static Controller 생성
```csharp
// 새 파일: PetTreeClimbingController.cs (static으로 변경)
public static class PetTreeClimbingController
{
    public static GameObject FindNearbyTree(PetController pet, float radius) { }
    public static Vector3 GetClimbPosition(GameObject tree) { }
}
```

### 4-3. ClimbTreeAction 통합
- [ ] 나무 찾기 로직 통합
- [ ] 나무 오르기 코루틴 통합
- [ ] NavMeshAgent 비활성화/활성화 로직 통합

### 4-4. 테스트
- [ ] 나무를 잘 찾는지
- [ ] 나무 오르기 애니메이션이 정상인지
- [ ] 나무 위에서 선택/들기가 정상 작동하는지
- [ ] 나무에서 내려오기가 정상인지

## 5단계: 최종 정리

### 5-1. PetController 정리
```csharp
private void Awake()
{
    // 제거할 컴포넌트들
    // - PetMovementController (인스턴스)
    // - PetFeedingController (인스턴스)
    // - PetSleepingController (인스턴스)
    // - PetTreeClimbingController (인스턴스)
    
    // 유지할 컴포넌트들
    animationController = gameObject.AddComponent<PetAnimationController>();
    interactionController = gameObject.AddComponent<PetInteractionController>();
    waterBehaviorController = gameObject.AddComponent<PetWaterBehaviorController>();
}
```

### 5-2. 전체 테스트
- [ ] 모든 Action 전환이 정상적인지
- [ ] 메모리 사용량이 감소했는지
- [ ] 성능이 개선되었는지
- [ ] 새로운 버그가 없는지

## 주의사항

1. **각 단계마다 충분한 테스트**
   - 최소 30분 이상 플레이 테스트
   - 여러 펫 동시 테스트

2. **Git 활용**
   - 각 단계 전후로 커밋
   - 문제 발생 시 `git revert` 또는 `git reset` 사용
   - 브랜치 활용: `git checkout -b refactoring`

3. **성능 모니터링**
   - Unity Profiler로 성능 측정
   - 특히 코루틴 수와 메모리 사용량 체크

4. **코드 리뷰**
   - 각 단계 완료 후 코드 리뷰
   - 특히 메모리 누수 가능성 체크

5. **파일 정리**
   - OLD 파일 생성하지 않기
   - Git이 모든 히스토리를 관리

## 예상 소요 시간

- 1단계 (WanderAction): 2-3시간
- 2단계 (EatAction): 3-4시간  
- 3단계 (SleepAction): 2-3시간
- 4단계 (ClimbTreeAction): 4-5시간
- 5단계 (최종 정리): 2시간

**총 예상 시간: 13-17시간**

## 성공 지표

- ✅ 파일 수 감소: 8개 → 4개
- ✅ 코드 중복 제거
- ✅ 더 직관적인 구조
- ✅ 성능 개선 또는 최소 동일
- ✅ 버그 없음