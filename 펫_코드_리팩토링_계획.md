# 펫 코드 리팩토링 계획

## 🔍 주요 문제점 및 개선 방안

### 1. **중복된 Activity 시스템**
- **현재 상황**: PetController와 PetAI에서 각각 Activity를 관리하고 있어 중복됨
- **문제점**: 
  - PetController의 `_allActivities`와 PetAI의 `availableActivities`가 동일한 역할
  - Activity 초기화 코드가 두 곳에서 중복
  - UpdateActivityAI() 메서드도 거의 동일한 로직
- **개선안**: PetAI로 Activity 관리를 완전히 통합

### 2. **상태 관리의 분산**
- **현재 상황**: 
  - PetState 클래스가 있지만 여전히 PetController에 많은 bool 플래그들이 존재
  - `isInteracting`, `isHolding`, `isSelected` 등이 PetController와 PetState에 중복
- **문제점**: 상태 동기화 문제 발생 가능
- **개선안**: 모든 상태를 PetState로 통합 관리

### 3. **컨트롤러 네이밍 불일치**
- **현재 상황**: 
  - Movement 관련: `PetMovementController`, `PetMovement` (Core에 별도 존재?)
  - Animation 관련: `PetAnimationController`, `PetAnimator` (Core에 별도 존재?)
- **개선안**: 역할이 명확한 하나의 컨트롤러로 통합

### 4. **욕구 관리 중복**
- **현재 상황**: 
  - PetNeeds에서 관리하지만 PetController에도 `hunger`, `sleepiness`, `affection` 변수 존재
  - 두 곳에서 동시에 업데이트하는 코드
- **개선안**: PetNeeds로 완전 통합

### 5. **Update 메서드의 복잡성**
- **현재 상황**: PetController의 Update가 너무 많은 일을 직접 처리
- **개선안**: 각 시스템이 자체적으로 Update를 처리하도록 분리

## 📋 단계별 리팩토링 계획

### **1단계: Activity 시스템 통합** ✅ **완료**
```csharp
// PetController에서 Activity 관련 코드 제거 - 완료
// PetAI가 모든 Activity 관리를 담당 - 완료
// PetController는 PetAI를 통해서만 Activity에 접근 - 완료
```

#### 주요 변경사항:
1. PetController의 `_allActivities`와 `_currentActivity` 변수 제거
2. `InitializeActivities()` 메서드를 제거하고 PetAI에서 처리
3. `UpdateActivityAI()` 메서드 제거 (PetAI의 Update에서 자동 처리)
4. PetAI 초기화 코드 추가:
   ```csharp
   petAI = gameObject.AddComponent<PetAI>();
   petAI.Init(this);
   ```
5. PetController에 `AI` 프로퍼티 추가로 외부 접근 가능
6. Activity 중단 시 `petAI.InterruptAndResetAI()` 호출

### **2단계: 상태 플래그 정리** ✅ **완료**
```csharp
// PetController의 중복 플래그들을 제거 - 완료
// PetState의 프로퍼티만 사용하도록 변경 - 완료
// 호환성을 위한 프로퍼티는 그대로 유지 - 완료
```

#### 주요 변경사항:
1. **PetInteractionController의 중복 플래그 제거**
   - `isSelected`, `isHolding` 로컬 변수 제거
   - 모든 참조를 `petController.isSelected`, `petController.isHolding`으로 변경

2. **욕구 관련 변수들을 PetNeeds로 이동**
   - `hunger`, `sleepiness`, `affection` 변수를 프로퍼티로 변경
   - `hungerIncreaseRate`, `sleepinessIncreaseRate` 등도 프로퍼티로 변경
   - PetNeeds에 setter 메서드와 프로퍼티 추가

3. **상태 관리 통합**
   - 모든 상태 플래그가 PetState를 통해 중앙 관리됨
   - 호환성을 위한 프로퍼티로 기존 코드가 계속 작동

#### 남은 작업:
- `isGatheringAnimationOverride`는 애니메이션 전용이므로 유지
- `highAffectionThreshold` 등 일부 설정은 PetController에 유지 (외부 참조가 많음)

### **3단계: 컨트롤러 통합** ✅ **완료**
```csharp
// PetMovementController + PetMovement → PetMovementController - 완료
// PetAnimationController + PetAnimator → PetAnimationController - 완료
// 중복 기능 제거 - 완료
```

#### 주요 변경사항:
1. **미사용 클래스 제거 (백업으로 변경)**
   - PetMovement.cs → PetMovement.cs.backup
   - PetAnimator.cs → PetAnimator.cs.backup
   - PetInteractor.cs → PetInteractor.cs.backup
   - PetEffects.cs → PetEffects.cs.backup
   - PetSensor.cs → PetSensor.cs.backup

2. **현재 컨트롤러 구조 유지**
   - 각 컨트롤러가 명확한 단일 책임을 가짐
   - 기능별로 잘 분리되어 있어 추가 통합 불필요

3. **컨트롤러 역할 정리**
   - **PetAnimationController**: 애니메이션 제어
   - **PetMovementController**: 이동 및 배회 로직
   - **PetFeedingController**: 먹이 탐색 및 섭취
   - **PetInteractionController**: 플레이어 상호작용
   - **PetSleepingController**: 수면 행동
   - **PetTreeClimbingController**: 나무 오르기
   - **PetWaterBehaviorController**: 물 관련 행동

### **4단계: 욕구 시스템 완전 분리** ✅ **완료**
```csharp
// PetController의 hunger, sleepiness, affection 변수 제거 - 완료
// 모든 욕구 관련 로직을 PetNeeds로 이동 - 완료
// PetNeeds가 자체 Update 메서드로 욕구 업데이트 - 완료
```

#### 주요 변경사항:
1. **인스펙터 표시용 변수 제거**
   - `_hungerDisplay`, `_sleepinessDisplay`, `_affectionDisplay` 제거
   - PetNeeds에 Range 속성 추가로 직접 조절 가능

2. **자동 업데이트 구현**
   - PetController의 `UpdateNeeds()` 메서드 제거
   - PetNeeds가 Unity의 Update에서 자체 처리

3. **이벤트 처리 간소화**
   - `OnNeedChanged`, `OnNeedCritical` 이벤트 핸들러 제거
   - 감정 표현만 직접 구독 유지

4. **프로퍼티 단순화**
   - 욕구 값 프로퍼티가 PetNeeds로 직접 위임

### **5단계: Update 로직 분산**
```csharp
// 각 시스템이 자체 Update 메서드 보유
// PetController는 시스템들의 Update만 호출
```

## 💡 즉시 적용 가능한 간단한 개선사항

1. **불필요한 null 체크 통합**
2. **중복된 상태 체크 로직을 메서드로 추출**
3. **매직 넘버를 상수로 변경**
4. **사용하지 않는 변수/메서드 제거**

## 🎯 기대 효과

- 코드가 훨씬 간단하고 유지보수하기 쉬워짐
- 각 시스템의 책임이 명확해짐
- 중복이 제거되어 버그 발생 가능성 감소
- 새로운 기능 추가 시 영향 범위를 쉽게 파악 가능

## ⚠️ 주의사항

- 각 단계별로 테스트를 진행하여 기존 기능이 정상 작동하는지 확인
- 호환성을 위한 프로퍼티는 당분간 유지
- 한 번에 모든 것을 변경하지 말고 단계적으로 진행